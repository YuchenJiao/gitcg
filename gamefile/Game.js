export const GITCG = {
  // Initialize the game state G
  setup: () => ({
    // obj: %method to initialize this object%
  }),

  // Function that updates G
  moves: {
    /**
     * func: ({ G, playerID }, params) => {
     *  G.obj(params) = playerID or other;
     * },
    **/
  },
};